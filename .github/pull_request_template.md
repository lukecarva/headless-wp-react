## What & why

<!-- What does this change do, and what problem does it solve? -->

## How to test

<!-- Steps a reviewer can follow. Include seed data or fixtures if relevant. -->

## Checklist

- [ ] `pnpm lint` and `pnpm typecheck` pass
- [ ] Tests added/updated and `pnpm test` passes
- [ ] PHP: `composer lint && composer analyse && composer test` pass
- [ ] GraphQL schema/codegen regenerated if the API changed
- [ ] Docs / ADR updated if a decision changed
